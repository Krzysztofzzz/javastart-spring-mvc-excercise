spring:
  h2:
    console:
      enabled: true
  datasource:
    url:
      jdbc:h2:mem:test
    username:
      username
    password:
      password
  jpa:
    show-sql: true #pokazanie wykonywanych zapytan sql w konsoli
    defer-datasource-initialization: true #opoznienie wykonania skryptu z pliku data.sql, poniewaz Spring spróbuje wykona? skrypt z pliku data.sql w momencie, w którym schemat bazy danych jeszcze nie zosta? wygenerowany.

